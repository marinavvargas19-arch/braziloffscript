import QuizDiscoveryClient from "@/components/quiz-discovery-client";

export const metadata = { title: "Which Brazil is calling you?" };

export default function Page() {
  return <QuizDiscoveryClient />;
}
